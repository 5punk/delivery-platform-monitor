module.exports = {
  // login to grubhub dashboard
  // https://restaurant.grubhub.com/dashboard/${ID}
  grubhubId: "", // prevent-sensitive-commit-string

  // login to doordash dashboard
  // https://www.doordash.com/merchant/summary?store_id=${ID}
  doordashId: "", // prevent-sensitive-commit-string

  // login to ubereats dashboard
  // https://restaurant.uber.com/home/${ID}
  ubereatsId: "" // prevent-sensitive-commit-string
};
